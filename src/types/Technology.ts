export interface Technology {
  name: string,
  description: string,
  description_en: string,
  hexColor: string,
  officialWebsite: string,
  logoUrl: string,
  isBackend: boolean,
  isFrontend?: boolean,
}